<!-- XML-QSTAT configuration file -->
<config>

<!--
    Change this value to reflect your local timezone relative to UTC.
    This offset is used when constructing RFC-3339 validated timestamps that
    look like this:

        1990-12-31T23:59:60Z
	1996-12-19T16:39:57-08:00

    The cocoon server will report proper local date time information,
    but we need to insert the UTC correction info for the RSS feed

    Setting "Z" means no correction (UTC time matches)

    CET (Central European Time) is 1 hour ahead of UTC, thus "+01:00"
    EST (East coast USA) is 5 hours behind UTC, thus "-05:00"

    Change the following param accordingly to your UTC offset.
-->
<UTC_TZ_offset>-05:00</UTC_TZ_offset>

<!--
  If the external alarm-threshold.xml file is present and this
  feature is enabled, xml-qstat can graphically show how close any
  given queue instance is to hitting a load based alarm state.
 
  Only handles the simple default case where the cluster queue
  load_thresholds parameter is based on a np_load_avg=<something> 
  rule. 

  Note: The create-threshold-config.pl in the scripts/ folder can
        be used to create the alarm-threshold.xml file. 
-->
<use-alarm-threshold>yes</use-alarm-threshold>
<alarm-threshold-file>alarm-threshold.xml</alarm-threshold-file>


<!--
    Set this to 'yes' if you have the qlicserver program
    and wish to view the resource allocation
-->
<qlicserver>no</qlicserver>

<!--
    Set this to 'yes' if you wish to use JavaScript
    may cause compatibility issues with some browser versions
-->
<useJavaScript>yes</useJavaScript>

<!--
    specify an external program for querying the the jobinfo
    - the jobId, etc. are passed via the queryString
-->
<programs>
  <!--
      specify an external program for obtaining the qstat -j information
      - leave empty if you don't have such a program, or if you wish
        to use the CommandGenerator
  -->
  <jobinfo></jobinfo>

  <!--
      specify an external program for viewing the stdout files
      (jobId, filenames, etc. are passed via the queryString)
      - leave empty if you don't have such a program
  -->
  <viewfile>http://127.0.0.1:8888/xmlqstat/viewfile</viewfile>

  <!--
      specify an external program for finding and viewing the log files
      (jobId, filenames, etc. are passed via the queryString)
      - leave empty if you don't have such a program
  -->
  <viewlog>http://127.0.0.1:8888/xmlqstat/logview</viewlog>

</programs>

<!--
    define a logo for all of the pages and the link location
    <topLogo src="images/xml-qstat-logo.png" width="150" />
-->

<topLogo src="images/xml-qstat-logo.png" width="150" href="http://bioteam.net" />


<!-- define cluster settings -->
<clusters>
  <cluster name="laptop" root="/opt/sge" cell="default"  />
</clusters>

</config>
<!-- end-of-file -->
